import  styled from 'styled-components';

interface HomeProps { }

const Home : React.FC<HomeProps> = (props) => {
    return (
        <div>
            <h1>Home</h1>
        </div>
    );
}

export default Home;